<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Teams</title>
    <link rel="stylesheet" href="../css/departmentTeam.css">
</head>
<body>
<div class="department-team-page">
    <h1 class="content__teams-list">Teams for {{ departmentInfo.department_name }}</h1>
    <div class="department-info">
        <span class="branch-name">Branch: {{ departmentInfo.branch_id }}</span>
    </div>
    <div class="teams-list">
        {% for team in teams %}
            <div class="team-item">
                <div id="team-id" style="visibility: hidden">{{ team.team_id }}</div>
                <a href="../teamUser.php?id={{ team.team_id|trim }}" class="team-id">{{ loop.index }}</a>
                <div class="team-container">
                    <span class="team-name">Team: {{ team.team_name }}</span>
                    <form method="post" action="../deleteDepartmentTeam.php">
                        <input type="hidden" name="department_id" value="{{ department_id }}">
                        <input type="hidden" name="team_id" value="{{ team.team_id|trim }}">
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this team?')">Delete</button>
                    </form>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="add-team-section">
        <h2>Add New Team</h2>
        <form method="post" action="../addDepartmentTeam.php">
            <input type="hidden" name="department_id" value="{{ department_id }}">
            <label class="add-team__label">
                Enter team name:
                <input type="text" name="team_name" class="label__team-name" required>
            </label>
            <button type="submit" class="add-team__button">Add Team</button>
        </form>
    </div>
</div>
</body>
</html>