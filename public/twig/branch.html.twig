<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Branch Departments</title>
    <link rel="stylesheet" href="../css/department.html.css">
</head>
<body>
<div class="branch-page">
    <h1 class="content__departments-list">Departments for {{ branchInfo.city }}</h1>
    <div class="branch-info">
        <span class="address">Address: {{ branchInfo.address }}</span>
        <span class="workers-count">Workers Count: {{ branchInfo.workers_count }}</span>
        <span class="branch-description">Description: {{ branchInfo.branch_description }}</span>
    </div>
    <div class="departments-list">
        {% for department in departments %}
            <div class="department-item">
                <div id="department-id" style="visibility: hidden">{{ department.department_id }}</div>
                <a href="../departmentTeam.php?id={{ department.department_id|trim }}" class="department-id">{{ loop.index }}</a>
                <div class="department-container">
                    <span class="department-name">Department: {{ department.department_name }}</span>
                    <form method="post" action="../deleteDepartment.php">
                        <input type="hidden" name="department_id" value="{{ department.department_id|trim }}">
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this department?')">Delete</button>
                    </form>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="add-department-section">
        <h2>Add New Department</h2>
        <form method="post" action="../branchDepartment.php?id={{ branch_id }}">
            <label class="add-department__label">
                Enter department name:
                <input type="text" name="department_name" class="label__department-name" required>
            </label>
            <button type="submit" name="add_department" class="add-department__button">Add Department</button>
        </form>
    </div>
</div>
<script type="module" src="../js/simplyScripts/addDepartment.js"></script>
</body>
</html>